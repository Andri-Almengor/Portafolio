{
  "name": "andrick-portfolio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "build": "npm --workspace client run build",
    "test": "npm --workspace server test",
    "setup:sheets": "npm --workspace server run setup:sheets",
    "generate:secrets": "node server/scripts/generate-secrets.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
